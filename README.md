# ChiNet Android Build Fix

Minimal Android WebView wrapper for https://chintbiruk.onrender.com/

This version intentionally uses only Android SDK APIs (no AndroidX dependencies) to avoid dependency-resolution/compile failures in GitHub Actions.

Build from GitHub Actions:
Actions -> Build ChiNet APK -> Run workflow.
